## The shipped [command_for_linux_audit] is `exe=.*\/(\S+)\"` — it requires a
## closing double quote after the path, which raw audit.log has and
## `ausearch -i` strips. This is the unquoted twin, tolerant of both.

[command_for_auditd]
REGEX = exe=(?:")?[^\s"]*\/([^\s"]+)
FORMAT = command::$1
